# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies and Build Outputs
node_modules
dist
dist-ssr
*.local
client/node_modules # Explicitly ignore client dependencies
server/node_modules # Explicitly ignore server dependencies
client/dist         # Ignore the Vite build output (if not 'dist')

---

# SECRETS AND ENVIRONMENT VARIABLES (NEW SECTION)
# Ignore all files named .env, regardless of location
.env
# Explicitly ignore common environment files
.env.local
.env.development.local
.env.test.local
.env.production.local
# Explicitly ignore files in your monorepo directories
client/.env
server/.env
# Also ignore common secret files
*.key
*.pem

---

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?